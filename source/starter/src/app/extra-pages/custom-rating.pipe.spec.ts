import { CustomRatingPipe } from './custom-rating.pipe';

describe('CustomRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
